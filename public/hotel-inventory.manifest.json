{
  "name": "Hotel supplier inventory manifest",
  "description": "Add public or signed CSV/JSON/JSONL supplier export URLs here. Use cities/provinces to let GitHub Pages load only matching destination shards.",
  "sources": [
  ]
}
